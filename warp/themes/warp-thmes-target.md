- The target dir is at `~/.local/share/warp-terminal/themes/` for actual theme queries

use `ln -s` to make a link for this `themes/` dir under `~/.warp`.

- so final path will be at:
    `~/.warp/themes/` 
    (in themeName.yaml + *optional* screenshot)
- and the source/target link will be at:
    `~/.local/share/warp-terminal/themes/`
